swagger: '2.0'
info:
  title: Rekognition
  version: '1.0'
  description: AWS Rekognition Image Processing API's
  contact:
    name: Leor Brenman
    email: lbrenman99@hotmail.com
host: 'localhost:3000'
schemes:
  - http
produces:
  - application/json
consumes:
  - application/json
paths:
  /detectLabels:
    post:
      summary: Detect Labels
      operationId: post-detectLabels
      responses:
        '200':
          description: OK
          schema:
            $ref: ../models/detectLabelResponse.yaml
      tags:
        - detectLabels
      description: Detect Labels in image
      security:
        - apikey: []
      parameters:
        - in: body
          name: body
          schema:
            $ref: ../models/image.yaml
definitions: {}
tags:
  - name: detectLabels
  - name: Rekognition
securityDefinitions:
  apikey:
    name: apikey
    type: apiKey
    in: header
